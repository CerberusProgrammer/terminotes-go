package commands

const (
	ErrExpectedAddCommand  = "expected 'add' command"
	ErrExpectedListCommand = "expected 'list' command"
	ErrContentRequired     = "Content is required"
	ErrEmptyCommands       = "No commands provided, so nothing to do"
	ErrUnknownCommand      = "Unknown command, I don't know what to do"
)
